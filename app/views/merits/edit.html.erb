<div class="container" style="margin-top:50px">
  <div class="panel panel-primary">
    <div class="panel-heading"><div class="panel-title" style="padding-top:1px;padding-bottom:1px"><h4 style="margin-left:5px">Merito</h4></div></div>
      <div class="panel-body">
      </div>
      <div class="panel-footer well">
        <%= form_for :merit, url: '/merit/update', html: { method: :put } do |f| %>
        <div class="row">
          <div class="col col-md-6">
            <%= f.label :Merito %><br />
            <%= f.text_field :name,:class=>"form-control"%>
          </div>
          <div class="col col-md-6">
            <%= f.label :fecha %><br>
            <%= f.date_select :date ,:start_year  =>1990, :end_year=>Time.now.year %>
          </div>
        </div>
        <%=f.hidden_field :id, :value => @merit.id%>
          <div class="actions">
            <%= f.submit "Guardar",:class =>"btn btn-primary pull-right"  %>
          </div>
          <br>
        <% end %>
      </div>
    </div>
  </div>
